<center>

<h1>home</h1>


<table border=1>
<tr> 

	<th>title</th>
	<th>body</th>
 
	</tr> 
         <% @posts.each do |post|%>

         <tr> 

         	<td><%= post.title%></td>
            <td><%= post.body%></td>
            

            <td><%= link_to "show", post_path(post.id)%></td><br>

             <td><%= link_to "edit", edit_post_path(post.id)%></td><br>
                  <td><%= button_to 'Destroy',post_path(post.id),method: :delete,data: { confirm: 'Are you sure?' } %></td>
         <%end%>   
           </tr>
           </table>
         <%= link_to 'Add post', new_post_path %>


</center>

 
<%#= render @posts %>
 
